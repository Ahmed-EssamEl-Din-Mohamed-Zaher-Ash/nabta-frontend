# Base URL of the Express backend. Leave EMPTY in development (the Vite proxy
# forwards /api to localhost:4000). In production set the deployed API origin,
# e.g. VITE_API_URL=https://nabta-api.onrender.com
VITE_API_URL=
